# 需求规格说明书

QQ 群聊虚拟群友  
与个人服务器管家

基于 NapCat + OneBot 11 + NoneBot2 的个人自托管方案

**文档版本：**v1.0

**状态：**技术选型与 MVP 需求基线

**面向对象：**个人自托管 / 单 QQ 机器人账号 / 少量 QQ 群

**调研日期：**2026-07-25

**建议技术栈：**NapCat · OneBot 11 · NoneBot2 · MySQL · Docker Compose

> **结论：**选择 OneBot 11 作为 NapCat 与业务机器人的通信协议，选择 NoneBot2 作为业务框架。MVP 先做好"群聊陪伴 + 白名单用户的确定性运维命令"，Hermes Agent 在第二阶段通过本机受保护 API 接入，仅允许白名单 QQ 号（群聊或私聊）触发 Hermes 进行服务器管理。

## 本次建议的三个边界

- NapCat 只负责 QQ 登录、收发消息和 OneBot API，不承载人格、数据库或运维业务。

- 群聊中的"听"默认是实时消费与短期上下文，不等于永久保存全部聊天记录。

- 服务器管理仅对白名单 QQ 号开放，白名单用户可通过群聊或私聊触发运维和 Hermes Agent；非白名单用户的任何消息均不能触发运维。

# 1. 背景与目标

本项目要构建一个长期在线、以 QQ 为主要入口的个人机器人。它在群聊里表现得像一个有分寸的"虚拟群友"：能理解一段对话的上下文，通常保持安静，在合适的时机偶尔吐槽或接话；对于白名单用户（无论群聊或私聊），它又是一个可审计、可控的服务器管理入口与 Hermes Agent 接口。系统后续还要承载 Hermes Agent、Minecraft 群服互联和数据库记忆等扩展。

## 1.1 产品目标

- 让机器人在指定 QQ 群中持续接收消息，并形成短期会话上下文。

- 用低打扰策略偶尔参与对话，保持人格一致，不刷屏、不抢话。

- 允许白名单用户通过群聊或私聊查询和管理自己的服务器，所有操作都有权限校验与审计。

- 通过插件化边界，使 QQ 接入、群聊人格、运维、Agent 和数据存储能独立替换。

- 以单机 Docker Compose 为默认部署形态，适合个人维护，不引入不必要的集群复杂度。

## 1.2 非目标

- 不做面向公众的大规模 SaaS 机器人平台。

- MVP 不提供任意用户创建插件、租户隔离或计费系统。

- MVP 不允许通过 QQ 直接执行任意 Shell、任意 SQL 或上传脚本。

- 不承诺绕过 QQ 的账号限制、登录策略变化或第三方协议风险。

- 不在第一阶段追求跨多个即时通讯平台；跨平台能力只保留接口。

## 1.3 成功标准

当机器人能稳定运行 7 天、群内不刷屏、白名单用户能安全完成一次服务器状态查询与一次带确认的重启，并且 NapCat 或 LLM 暂时不可用时不会拖垮其他模块，即认为 MVP 达标。

# 2. 技术选型结论

## 2.1 协议与框架

| **层次**  | **选择**          | **理由**                                                                  | **保留项**                           |
|-----------|-------------------|---------------------------------------------------------------------------|--------------------------------------|
| QQ 接入端 | NapCat            | 基于 NTQQ，完整实现 OneBot 11，并支持 HTTP、WebSocket 与反向 WebSocket。  | 独立进程部署，可滚动升级。           |
| 线协议    | OneBot 11         | 与 NapCat 的成熟能力直接匹配，NoneBot2 有正式适配器，生态广。             | 业务内部不直接依赖 CQ 码细节。       |
| 连接方式  | 反向 WebSocket    | NoneBot2 官方适配器将其列为推荐方式；事件与 API 可在单连接中完成。        | 同主机绑定 127.0.0.1；容器内走私网。 |
| 业务框架  | NoneBot2          | Python 异步、插件/Matcher/权限模型成熟，官方 OneBot 适配和 ORM 路径清楚。 | 插件只依赖应用服务接口。             |
| 数据库    | MySQL 8 / MariaDB | 满足长期信息、审计、配置和后续群服数据持久化。                            | 开发测试可用 SQLite。                |
| 部署      | Docker Compose    | 个人维护成本低，组件边界清晰，便于备份和重启。                            | 不引入 Kubernetes。                  |

## 2.2 为什么不优先选其他方案

| **候选**          | **优点**                                                             | **主要问题**                                                                                           | **结论**         |
|-------------------|----------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------|------------------|
| NoneBot2          | Python 生态；正式 OneBot 适配；Matcher 权限与插件化；官方 ORM 实践。 | 需要自己做管理界面与产品层结构。                                                                       | 推荐             |
| Koishi            | 控制台体验好，插件生态与配置友好。                                   | 官方 OneBot adapter 包已被弃用，NapCat 主要依赖社区适配；与 Hermes 的 Python 侧衔接不如 NoneBot 直接。 | 备选             |
| NapCat SDK / 直连 | 代码少、上手快、类型提示可好。                                       | 权限、插件生命周期、数据库迁移、测试与跨平台抽象都要自己补。                                           | 只适合 PoC       |
| QQ 官方机器人 API | 平台合规路径更明确。                                                 | 面向普通个人 QQ 群及既有账号的能力边界与 NapCat 使用场景不同。                                         | 不作为本项目主线 |

> **选型原则：**“协议”与“框架”是两件事：OneBot 11 解决 NapCat 和机器人如何通信；NoneBot2 解决业务如何组织。把两者混在一起，会导致今后更换 QQ 接入端或增加其他聊天平台时重写全部业务。

# 3. 用户、角色与典型场景

## 3.1 角色

| **角色**   | **身份来源**                                      | **可用能力**                                   | **禁止能力**                            |
|------------|---------------------------------------------------|------------------------------------------------|-----------------------------------------|
| 白名单用户 | 配置中的 WHITELIST_QQ_IDS（支持多个 QQ 号）；群聊或私聊上下文均生效 | 全部查询；受控运维；Hermes Agent 对话；审批高风险任务 | 默认仍不能绕过命令白名单和二次确认      |
| 群管理员   | 指定群的管理员                                    | 可调整本群静音、吐槽频率、人格开关             | 不能管理服务器或访问私人记忆            |
| 普通群员   | 指定 QQ 群成员                                    | 参与群聊、@机器人、使用公开命令                | 不能触发运维、查看审计或读取私聊        |
| 系统服务   | 内部服务身份 + 服务凭据                           | NapCat、数据库、运维网关、Hermes 之间调用      | 不得共享白名单凭据；最小权限            |

## 3.2 关键用户故事

- 作为群员，我希望机器人多数时间安静，偶尔能顺着大家正在聊的内容接一句，而不是每条消息都回复。

- 作为群员，我 @ 机器人或回复它时，希望它能明确回应，并能理解最近几条上下文。

- 作为白名单用户，我在群聊或私聊中发送"/服 状态"时，希望立即看到在线状态、玩家数、TPS/MSPT、内存和最近一次备份时间。

- 作为白名单用户，我在群聊或私聊中发送"/服 重启"时，希望机器人先展示影响范围并生成一次性确认码；确认后执行，完成后回报结果。

- 作为白名单用户，我以后可以通过群聊或私聊把自然语言任务交给 Hermes Agent，但危险命令仍需二次确认。

- 作为服主，我以后能把 Minecraft 聊天、上线/下线和告警同步到指定 QQ 群，并防止双向消息回环。

## 3.3 使用假设

初始规模假设为 1 个机器人账号、1–5 位白名单用户、1–10 个 QQ 群、每群日消息不超过约 10,000 条、1–5 台被管服务器。若实际规模显著高于此值，需要重新评估上下文缓存、任务队列和数据库索引。

# 4. 范围与版本划分

| **能力**   | **MVP（必须）**                                        | **P1（近期）**                    | **P2（后续）**               |
|------------|--------------------------------------------------------|-----------------------------------|------------------------------|
| QQ 接入    | NapCat + OneBot 11 反向 WS；断线重连；收发群/私聊      | 多群配置；消息类型补全            | 备用接入端或多平台           |
| 虚拟群友   | 短期上下文；@必答；低概率主动吐槽；限频                | 人格配置；话题/情绪门控；摘要记忆 | 自适应风格与跨群差异         |
| 服务器管理 | 白名单 QQ 身份校验；确定性命令；群聊/私聊双通道；帮助与审批 | 自然语言意图转命令            | Hermes Agent 完整接入        |
| 服务器运维 | 状态、玩家、日志尾部、启动/停止/重启、备份；高风险确认 | 告警、计划任务、回滚              | 多种游戏服/数据库运维技能    |
| 群服互联   | 预留接口                                               | Minecraft ↔ QQ 聊天、事件、告警   | 多服路由、身份绑定、跨服频道 |
| 数据       | MySQL 配置、审计、操作任务；消息短期缓存               | 群摘要和长期记忆                  | 向量检索/知识库（有必要时）  |

> **MVP 的重要取舍：**"会聊天"和"会运维"采用两条路径：群聊人格可以调用 LLM；运维命令必须先落到确定性的命令解析与白名单动作。不要让一次自然语言生成直接变成 Shell。白名单用户在群聊中的运维命令同样受此约束，不会因为来源是群聊就降低确定性解析要求。

# 5. 功能需求：群聊虚拟群友

## 5.1 消息接收与上下文

- **FR-G01：**接收指定群的全部群消息事件，包括文本、@、回复、图片占位、表情和文件元数据；忽略机器人自身消息。

- **FR-G02：**按 group_id 维护独立滚动上下文，默认保留最近 30 条或 20 分钟，先到者淘汰。

- **FR-G03：**上下文只保存生成回复所需的规范化信息：发送者标识、昵称快照、时间、文本/消息类型和 reply_to；不默认下载媒体原件。

- **FR-G04：**NapCat 断线时不阻塞数据库和运维任务；重连后从新事件继续，不承诺补齐所有离线群消息。

- **FR-G05：**同一 message_id 必须幂等处理，避免断线重发或多插件导致重复回复。

## 5.2 回复触发模型

采用“硬触发 + 软触发 + 总闸门”的两阶段策略。第一阶段只做本地快速判断，只有候选消息才调用 LLM，从而控制费用、延迟和隐私暴露。

| **触发类型** | **例子**                                                   | **默认行为**                         |
|--------------|------------------------------------------------------------|--------------------------------------|
| 硬触发       | @机器人、回复机器人、以机器人昵称开头、公开命令            | 在权限和冷却允许时必答；目标 15 秒内 |
| 软触发       | 出现明显槽点、连续讨论同一话题、长时间沉默后有人抛梗       | 通过候选评分后再按概率抽样           |
| 禁止触发     | 群内运维词、疑似隐私、争吵升级、机器人互相触发、短时间刷屏 | 保持沉默；必要时记录原因             |
| 总闸门       | 群静音、夜间静默、全局维护、当小时额度耗尽                 | 任何软触发均不回复                   |

## 5.3 默认吐槽参数

- 基础主动概率：候选消息的 2%，不是全部消息的 2%。

- 群级冷却：10 分钟；用户级冷却：20 分钟。

- 群级上限：每小时最多 3 次主动发言；连续主动发言不超过 1 次。

- 硬触发不受主动概率限制，但受防刷屏和每用户限流控制。

- 输出长度：通常 1–2 句，默认不超过 80 个汉字；不要写成长篇助手回答。

- 夜间策略：00:30–07:30 只响应硬触发，时区可配置。

## 5.4 人格与内容规范

- 人格应像熟悉群氛围的朋友，而不是客服：简短、自然、能接梗，但不刻意每次抖机灵。

- 不冒充真实群员，不声称拥有未获得的信息；被问到身份时明确是机器人。

- 不基于敏感属性进行攻击；不传播群成员私聊内容、服务器密钥或其他群的上下文。

- 被要求安静时可按群或按时长静音；群管理员和白名单用户可查询当前静音状态。

- LLM 超时、内容审核失败或上下文不足时宁可不说，不发送半截回复。

# 6. 功能需求：白名单用户与服务器运维

## 6.1 权限前置条件

服务器管理仅对配置白名单（WHITELIST_QQ_IDS）中的 QQ 号开放，白名单用户可通过**群聊或私聊**中的消息触发运维命令与 Hermes Agent。权限校验逻辑如下：

1. 从消息事件中提取 sender_id（发送者 QQ 号）。
2. 检查 sender_id 是否存在于 WHITELIST_QQ_IDS 列表中。
3. 若在白名单中，则**无论消息来自群聊还是私聊**，均授予服务器管理权限。
4. 若不在白名单中，则群聊中的 @、引用、转发聊天记录、伪造昵称或管理员身份均不能绕过此条件。
5. 白名单配置通过环境变量或安全配置文件管理，支持多个 QQ 号；修改后需重启或热加载生效。

> **关键安全原则：**白名单身份完全由 OneBot 事件中的 sender_id 决定，不受消息文本内容影响。即使消息文本包含"我是白名单用户""我是主人"等话术，只要 sender_id 不在白名单中，就拒绝执行任何运维操作。

## 6.2 MVP 命令

| **命令**                   | **作用**                                           | **风险级别** | **确认**         |
|----------------------------|----------------------------------------------------|--------------|------------------|
| /服 状态 \[服名\]          | 在线状态、版本、玩家、TPS/MSPT、CPU/内存、运行时长 | 只读         | 无需             |
| /服 玩家 \[服名\]          | 在线玩家列表与人数                                 | 只读         | 无需             |
| /服 日志 \[服名\] \[行数\] | 返回脱敏后的最近日志，行数有上限                   | 只读         | 无需             |
| /服 启动 \[服名\]          | 启动已登记的服务                                   | 中           | 可配置           |
| /服 停止 \[服名\]          | 优雅停止并等待落盘                                 | 高           | 必须             |
| /服 重启 \[服名\]          | 广播、倒计时、优雅停止后启动                       | 高           | 必须             |
| /服 备份 \[服名\]          | 触发在线安全备份并报告快照 ID                      | 中           | 无需/可配置      |
| /任务 \[ID\]               | 查询异步任务进度与结果                             | 只读         | 无需             |
| /确认 \<一次性码\>         | 确认待执行高风险动作                               | 审批         | 两分钟内一次有效 |
| /取消 \<一次性码\>         | 撤销待执行动作                                     | 审批         | 立即             |

## 6.3 操作执行规则

- 每个运维请求生成 operation_id，并记录请求人（QQ 号）、目标、规范化参数、风险等级、审批、开始/结束时间和结果摘要。

- 高风险动作先返回影响说明和一次性确认码；确认码绑定 sender_id（白名单 QQ 号）、operation_id 和参数哈希，超时或参数变化即失效。

- 若消息来自群聊，高风险操作的确认响应**同样发到群聊**（因为白名单用户在群中发送的命令应在同一上下文中完成审批），并附带一次性确认码。群聊中的确认消息同样需要 sender_id 在白名单中才有效。

- 执行层只接受结构化动作，例如 restart_server(server_id)，不接受来自 QQ 的原始 Shell 字符串。

- 同一服务器的互斥操作串行化；已有重启/备份任务时返回当前任务而不是重复执行。

- 操作超时后标记为 unknown 并主动检查真实状态，不能简单回复“失败”后放任后台继续运行。

- 所有回包先做敏感信息脱敏：路径、IP、令牌、数据库 DSN、玩家 IP 和环境变量不得原样发到 QQ。

## 6.4 Minecraft 控制接口

优先顺序建议为：Paper 的 Management Protocol（若目标版本稳定支持）或受限的本地管理服务 \> 容器/系统服务 API \> RCON。无论采用哪种方式，接口都只暴露项目所需动作，不给机器人通用管理员权限。Paper 文档中的 Management Protocol 支持 localhost 绑定、独立 secret 与 TLS，可作为后续方案验证。

# 7. 总体架构

系统采用"接入层—机器人应用层—能力层—资源层"四层结构。组件可以同机部署，但逻辑凭据和网络权限必须分离。

| **层** | **组件**                                  | **职责**                                          | **不得承担**                        |
|--------|-------------------------------------------|---------------------------------------------------|-------------------------------------|
| 接入层 | NapCat / NTQQ                             | QQ 登录、事件上报、消息与群管理 API               | 人格、数据库业务、服务器 Shell      |
| 应用层 | NoneBot2                                  | 事件规范化、Matcher、权限校验（白名单）、插件生命周期、回复编排 | 保存 NapCat 登录凭据；直接持有 root |
| 能力层 | persona / admin / ops / agent_bridge 插件 | 群聊门控、确定性命令（群聊/私聊双通道）、白名单校验、任务编排、Hermes 代理 | 跨插件直接读写私有表                |
| 服务层 | Ops Gateway / Hermes API                  | 白名单运维动作；Agent 会话与工具执行              | 公开监听互联网；接受非白名单身份    |
| 资源层 | MySQL / 游戏服 / 备份存储                 | 持久化与被管资源                                  | 直接向 QQ 暴露端口                  |

## 7.1 事件流：群聊吐槽

1.  NapCat 将群消息通过反向 WebSocket 推送给 NoneBot2。

2.  event_ingest 完成去重、规范化、群白名单和基础敏感内容标记。

3.  context_store 将消息放入该群的短期窗口。

4.  persona_gate 计算硬触发/软触发、冷却、额度和静音。

5.  只有候选事件进入 LLM；输出通过长度、泄密和安全检查后发回群。

6.  记录触发原因、耗时和是否发送，不默认保存完整提示词。

## 7.2 事件流：白名单用户重启服务器

1.  NoneBot2 校验 sender_id 是否在 WHITELIST_QQ_IDS 中，并通过速率限制。

2.  无论事件来自群聊还是私聊，admin_command 将文本解析为 restart(server_id)，查询目标是否已登记。

3.  risk_policy 判定为高风险，创建待审批 operation 并返回一次性码。

4.  白名单用户发送 /确认；系统再次校验身份（sender_id 仍须在白名单中）、码、时效和参数哈希。

5.  ops_gateway 执行广播、倒计时、优雅重启与健康检查。

6.  机器人将最终状态发回原会话（群聊或私聊）；审计表保留完整状态机。

> **注：**若命令来自群聊，确认码和后续状态消息也发到同一群聊。群聊中的确认消息同样只信任事件中的 sender_id，不信任消息文本内容。

## 7.3 插件边界

> plugins/event_ingest \# OneBot 事件规范化、幂等与路由
>
> plugins/persona \# 上下文、触发门控、LLM 回复与群级配置
>
> plugins/admin_command \# 白名单校验、命令解析、审批交互、群聊/私聊双通道路由
>
> plugins/server_ops \# 结构化运维任务客户端，不含通用 Shell
>
> plugins/agent_bridge \# Hermes API 会话、流式进度与审批映射（P2）
>
> plugins/game_bridge \# Minecraft ↔ QQ 消息与事件桥接（P1）
>
> core/services \# 权限、审计、配置、任务、模型供应商抽象

# 8. Hermes Agent 接入设计（P2）

Hermes Agent 当前提供 OpenAI 兼容的本机 HTTP API，并可使用 local、Docker、SSH 等终端后端；还提供 MCP、工具集过滤、命令审批、容器隔离和消息平台身份控制。因此建议把 Hermes 作为独立进程，通过受保护 API 接入，而不是在 NoneBot 插件中启动一个交互式终端。

## 8.1 白名单准入原则

Hermes Agent 的接入权限与服务器运维权限共用同一套白名单机制：

- **准入条件：**仅当消息事件的 sender_id 在 WHITELIST_QQ_IDS 中时，agent_bridge 插件才会将 QQ 会话映射为 Hermes session。
- **会话来源：**白名单用户可通过**群聊或私聊**发起 Hermes Agent 会话。群聊中的会话同样只受 sender_id 白名单校验，不受消息场景影响。
- **非白名单用户：**任何群聊 @、回复、转发消息或伪造话术均无法创建 Hermes 工具会话。

## 8.2 推荐集成

- Hermes API 仅绑定 127.0.0.1 或 Docker 私网，启用 API_SERVER_KEY；QQ 侧凭据存于 secrets，不写数据库和日志。

- agent_bridge 只允许白名单用户访问，将 QQ 会话（群聊或私聊）映射为独立 Hermes session。

- Hermes 使用 Docker backend 或专用低权限系统用户；工作目录限制在运维工作区。

- 服务器能力优先封装成 Ops MCP/受控 API，只向 Hermes 暴露 status、backup、restart 等细粒度工具。

- Hermes 的危险命令审批映射回 QQ（原会话所在群聊或私聊）；审批消息必须显示规范化命令、目标、风险和有效期。

- LLM/Agent 返回的文本不能自行视为执行成功；执行结果只信任工具调用的结构化状态。

## 8.3 明确禁止

- 禁止把 Hermes API 暴露在公网且只靠弱口令保护。

- 禁止让非白名单用户的群聊内容直接进入带 terminal 工具集的 Hermes 会话。

- 禁止给 Hermes 宿主机 root、Docker socket 全权限或无限制 sudo。

- 禁止把 QQ 消息拼接成 shell=True、eval、SQL 字符串或模板命令。

- 禁止省略工具结果核验，仅根据 Agent 的自然语言回复判定任务完成。

| **落地判断** Hermes 官方 API Server 已经是"带完整工具集的 Agent 运行时"，不是普通聊天模型代理。调用它等于把任务交给能执行工具的进程，因此网络隔离、工具集裁剪、白名单准入和审批是产品需求，不是部署细节。 |
|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|

# 9. 数据与存储需求

## 9.1 数据对象

| **对象**             | **关键字段**                                                             | **保留策略**                    |
|----------------------|--------------------------------------------------------------------------|---------------------------------|
| group_config         | group_id、enabled、persona、reply_budget、quiet_hours、admin_ids         | 长期，配置变更留审计            |
| message_context      | group_id、message_id、sender_alias、type、normalized_text、created_at    | 默认内存/Redis；30 条或 20 分钟 |
| conversation_summary | scope、period、summary、source_range、safety_label                       | P1 可选；群级可关闭             |
| server_target        | server_id、display_name、driver、endpoint_ref、capabilities              | 长期；凭据仅存 secret 引用      |
| operation_job        | operation_id、actor、action、target、state、approval、timestamps、result | 至少 180 天                     |
| audit_event          | event_type、actor、scope、decision、reason、correlation_id               | 至少 180 天；不写密钥           |
| user_binding         | qq_id、game_uuid、display_name、verified_at                              | 群服互联启用后长期              |

## 9.2 消息留存原则

默认不把所有群消息永久写入 MySQL。系统为了"听懂正在聊什么"只需要短期滚动上下文；长期人格记忆应优先保存摘要、群内明确设定和白名单用户确认的信息。若以后要保存原始消息用于搜索，应按群显式开启、说明用途、设置保留期和删除机制。

## 9.3 数据库实现

- 使用 nonebot-plugin-orm，底层 SQLAlchemy + Alembic；生产 DSN 使用 mysql+aiomysql。

- 所有表使用 UTC 时间存储，展示时按配置时区转换。

- 数据库迁移纳入部署流程：升级前备份，执行 migration，运行 schema check 后再启动机器人。

- 操作审计与业务数据分表；日志中使用 correlation_id 关联，不复制完整消息。

- 数据库账号只授予本项目 schema 的必要权限，不使用 root。

# 10. 群服互联需求（P1）

## 10.1 Minecraft → QQ

- 玩家上线/下线、死亡、成就、服务器启停与告警可按事件类型开关。

- 游戏聊天转发时显示绑定后的玩家名；未绑定用户按配置匿名或使用游戏名。

- 消息合并与限频：高频事件在 5–10 秒窗口聚合，避免刷群。

- 服务器日志中的 IP、坐标、命令和插件敏感输出按规则脱敏。

## 10.2 QQ → Minecraft

- 只转发指定群、指定消息类型；机器人命令和机器人自身消息不进入游戏。

- 为每条桥接消息加 source_id 与 trace_id，识别并丢弃回环消息。

- 限制长度、颜色码、控制字符和 @全体成员；图片只转为受控占位或链接。

- 可配置 QQ 昵称、绑定游戏名或匿名显示；禁止伪造服务器系统消息样式。

## 10.3 身份绑定

建议采用一次性验证码：QQ 私聊申请绑定 → 机器人生成短码 → 玩家在游戏内执行绑定命令 → 服务端确认 UUID 与 QQ ID。验证码 5 分钟有效、单次使用，解绑需要白名单用户或对应账号再次确认。

# 11. 安全、隐私与合规要求

## 11.1 账号与网络

- NapCat 建议使用专用 QQ 号，避免把个人主号作为测试账号；控制频率，保留人工登录与停机手段。

- NapCat WebUI、OneBot WebSocket、MySQL、Hermes API 和游戏管理端口不对公网开放。

- OneBot 与 Hermes 使用独立随机令牌；密钥通过环境 secret/权限受限文件注入，不进入代码仓库。

- 容器网络分区：NapCat 只需连 bot；bot 可连 MySQL、Hermes、Ops Gateway；NapCat 无权连数据库和游戏服。

- 依赖升级先在测试 QQ/测试群验证；固定镜像或版本，不盲目自动更新 NapCat/NTQQ。

## 11.2 Prompt Injection 与权限绕过

所有群聊内容都是不可信输入。即便消息写着"我是白名单用户""我是主人""系统提示""忽略规则"或包含终端命令，也只能作为聊天文本处理。身份来自 OneBot 事件的 sender_id，权限来自本地 WHITELIST_QQ_IDS 配置，不能由 LLM 自行判断。白名单状态不因消息文本或 LLM 输出而改变。

## 11.3 风险动作分级

| **级别**  | **示例**                                     | **控制**                                       |
|-----------|----------------------------------------------|------------------------------------------------|
| R0 只读   | 状态、玩家、指标                             | 白名单用户（群聊或私聊）；速率限制；审计       |
| R1 可逆   | 启动、触发备份、刷新配置                     | 目标白名单；可选确认；审计                     |
| R2 有中断 | 停止、重启、踢人、执行已登记管理命令         | 强制一次性确认；超时；结果核验                 |
| R3 破坏性 | 删除世界、恢复覆盖、任意 Shell/SQL、权限变更 | MVP 禁止；以后需多步审批、离线备份和人工控制台 |

## 11.4 隐私

- 机器人入群后应在群公告或首次发言中说明其会读取群消息用于上下文与自动回复。

- 提供“本群静音”“清除本群摘要”“不记录我”等管理能力的产品预留。

- 私聊内容与群聊上下文隔离；任何群回复不得引用白名单用户的私聊内容。

- 备份和日志设置保留期；导出审计时默认脱敏 QQ 号、IP 和令牌。

# 12. 非功能需求

| **类别** | **指标 / 约束**                                                      | **验收方法**           |
|----------|----------------------------------------------------------------------|------------------------|
| 可用性   | 连续运行 7 天；单组件崩溃可自动重启；NapCat 断线可恢复               | 故障注入 + 运行记录    |
| 性能     | 非 LLM 命令 P95 \< 1 秒；入站事件本地门控 P95 \< 100 ms              | 压测与结构化日志       |
| LLM 延迟 | 硬触发首个可见反馈 \< 3 秒；最终回复目标 \< 15 秒；超时则静默/提示   | 模拟慢模型与断网       |
| 可靠性   | message_id 幂等；运维任务有状态机；重复确认不重复执行                | 重放事件与并发测试     |
| 安全     | 非白名单用户无法触发任何运维和 Hermes 会话；密钥不出现在 QQ、日志、数据库 | 白名单注入测试、权限矩阵与 secret 扫描 |
| 可观测性 | 连接、事件、触发、LLM、任务、错误均有 correlation_id；健康检查可查询 | 日志与 /health 验收    |
| 可维护性 | 插件之间通过接口；配置带 schema；数据库迁移可回滚                    | 代码评审与升级演练     |
| 成本     | 全部消息不逐条调用 LLM；按群统计调用量、token 和失败率               | 月度指标报表           |

## 12.1 降级策略

- LLM 不可用：@回复“暂时脑子短路”，主动吐槽全部关闭；运维命令继续可用。

- MySQL 不可用：停止创建运维写操作；只允许可安全获取的实时只读状态；群聊可退化为无记忆模式。

- Hermes 不可用：确定性命令继续工作；Agent 请求明确提示不可用，不自动改走 Shell。

- NapCat 不可用：后台运维任务继续完成并落审计，连接恢复后只推送仍有意义的结果。

- Ops Gateway 不可用：不尝试 SSH fallback；返回目标不可达并保留诊断 ID。

# 13. 部署与配置基线

## 13.1 服务清单

| **服务**     | **建议运行身份**       | **持久卷**                      | **网络**                        |
|--------------|------------------------|---------------------------------|---------------------------------|
| napcat       | 独立非 root            | QQ/NapCat 配置与必要缓存        | 仅 bot-net；WebUI 仅 localhost  |
| bot          | 独立非 root            | 配置、插件数据、临时文件        | bot-net + data-net + ops-net    |
| mysql        | 数据库用户             | 数据库卷 + 备份                 | 仅 data-net                     |
| ops-gateway  | 每类资源专用低权限用户 | 任务状态/必要日志               | 仅 ops-net；可访问被管目标      |
| hermes（P2） | 独立低权限或容器       | profile、session、skills 工作区 | 仅 agent-net；API 仅 bot 可访问 |

## 13.2 关键配置项

> WHITELIST_QQ_IDS=\<白名单QQ号列表，以逗号分隔；例如：123456,789012\>
>
> ONEBOT_ACCESS_TOKEN=\<高强度随机令牌\>
>
> ONEBOT_REVERSE_WS=ws://bot:8080/onebot/v11/ws
>
> SQLALCHEMY_DATABASE_URL=mysql+aiomysql://bot:\*\*\*@mysql/bot
>
> PERSONA_ACTIVE_PROBABILITY=0.02
>
> PERSONA_GROUP_COOLDOWN_SECONDS=600
>
> PERSONA_MAX_ACTIVE_REPLIES_PER_HOUR=3
>
> CONTEXT_MAX_MESSAGES=30
>
> CONTEXT_TTL_SECONDS=1200
>
> HERMES_API_BASE=http://hermes:8642/v1 \# P2
>
> HERMES_API_KEY=\<独立随机令牌\> \# P2

## 13.3 备份

至少备份 MySQL、机器人配置、server_target 定义、Hermes profile/skills（启用后）和 Minecraft 世界。NapCat/QQ 会话文件是否备份需谨慎评估安全性；备份本身必须加密并限制读取。每月做一次实际恢复演练，而不是只确认备份任务“成功”。

# 14. 里程碑与实施计划

| **阶段**      | **建议工期** | **交付物**                                                       | **退出条件**                         |
|---------------|--------------|------------------------------------------------------------------|--------------------------------------|
| P0 连通 PoC   | 1–2 天       | NapCat、NoneBot2、反向 WS；收发群/私聊；Docker Compose 骨架      | 稳定收发；令牌与本机绑定生效         |
| P1 MVP        | 1–2 周       | 短期上下文、吐槽门控、群配置、MySQL、白名单命令、Ops Gateway、审计 | 第 15 章 MVP 验收全部通过            |
| P1.5 群服互联 | 约 1 周      | Minecraft 事件/聊天桥、身份绑定、回环防护                        | 双向消息不回环；限频生效             |
| P2 Hermes     | 1–2 周       | Hermes API bridge、会话、工具集裁剪、QQ 审批、隔离运行           | 注入测试不能绕过权限；危险动作有审批 |
| P3 稳定化     | 持续         | 告警、备份恢复、版本升级手册、成本与行为调优                     | 连续运行与恢复演练达标               |

以上工期以一名熟悉 Python/Docker 的开发者、已有可用服务器为假设，不包含 QQ 登录异常、Minecraft 版本兼容和 LLM 供应商申请时间。

## 14.1 推荐实施顺序

1.  先搭 NapCat ↔ NoneBot2 反向 WebSocket，只实现 echo 与身份日志。

2.  加入事件规范化、群聊/私聊统一的白名单校验（WHITELIST_QQ_IDS）。

3.  实现确定性的运维命令与模拟 Ops Gateway，先不接真实服务器。

4.  接 MySQL、审计状态机和高风险确认，再连接测试 Minecraft 服。

5.  实现群聊短期上下文和本地门控，最后接 LLM 并调低主动频率。

6.  完成 7 天稳定性测试后，再做群服互联与 Hermes。

# 15. 验收标准

## 15.1 MVP 验收

| **ID** | **场景**                                         | **预期结果**                                                       |
|--------|--------------------------------------------------|--------------------------------------------------------------------|
| AC-01  | 指定群连续发送 100 条普通消息                    | 全部被上下文消费；机器人主动回复不超过配置额度；无重复回复         |
| AC-02  | 群员 @ 机器人                                    | 机器人结合最近上下文回应；不暴露其他群或私聊内容                   |
| AC-03  | 非白名单群员发送"重启服务器"或伪造白名单用户话术 | 只作为聊天；无 operation_job 被创建                                |
| AC-04  | 白名单用户私聊 /服 状态                          | 1 秒内返回受控状态或任务受理；无 secret/IP 泄露                    |
| AC-05  | 白名单用户在群聊中发送 /服 重启                  | 先返回影响和确认码；未确认不执行；确认后只执行一次                 |
| AC-06  | 重复发送 /确认                                   | 第二次返回已处理，不重复重启                                       |
| AC-07  | MySQL 中断                                       | 高风险新操作被拒绝；机器人不悄悄无审计执行                         |
| AC-08  | LLM 超时/返回违规文本                            | 主动回复被丢弃或安全降级；运维仍可用                               |
| AC-09  | NapCat 断线并恢复                                | 机器人自动恢复接收；后台任务不被重复执行                           |
| AC-10  | 检查日志、数据库与 QQ 消息                       | 无 OneBot token、Hermes key、数据库密码或原始环境变量              |
| AC-11  | 连续运行 7 天                                    | 无不可恢复崩溃；资源占用无持续增长；错误可通过 correlation_id 追踪 |
| AC-12  | 非白名单用户在任何场景发送运维命令               | 运维命令不被执行；无审计记录创建；机器人仅作普通聊天回应或无回应   |
| AC-13  | 白名单用户在群聊中发送高风险命令（如 /服 重启）  | 确认码在群聊中展示；仅在同一个群聊中确认才有效；其他群员无法代为确认 |

## 15.2 Hermes 阶段附加验收

- 非白名单用户（包括普通群员、群管理员）在任何场景（群聊、私聊、@、转发）均无法创建 Hermes 工具会话。

- 白名单用户可在群聊或私聊中发起 Hermes Agent 会话，且身份校验仅依赖 sender_id。

- Prompt injection 测试不能改变 WHITELIST_QQ_IDS、工具白名单或审批策略。

- Hermes 在 Docker/低权限后端中无法读取宿主机不相关目录和凭据。

- 高风险工具调用会在 QQ（原会话群聊或私聊）显示结构化预览；拒绝或超时后不执行。

- Agent 文本声称"已完成"但工具未返回成功时，系统状态仍为失败/未知。

# 16. 风险、约束与待确认项

## 16.1 主要风险

| **风险**                 | **影响**                 | **应对**                                                      |
|--------------------------|--------------------------|---------------------------------------------------------------|
| NapCat/NTQQ 版本变化     | 登录、消息或接口失效     | 专用 QQ；固定版本；测试环境先升级；保留停机开关               |
| 账号风控                 | 机器人号受限或掉线       | 低频行为；避免群发/刷屏；人工观察；接受无法完全消除           |
| 白名单 QQ 号泄露或滥用   | 非授权用户获取服务器管理 | 白名单严格控制；定期审计操作记录；高风险操作需二次确认        |
| LLM 群聊失控             | 刷屏、冒犯、泄密         | 两阶段门控、短输出、安全过滤、群级总闸门                      |
| 自然语言运维误判         | 误重启或破坏数据         | MVP 确定性命令；结构化 action；高风险二次确认                 |
| Hermes 权限过大          | 宿主机或其他服务被影响   | 容器/低权限、工具裁剪、MCP/受控 API、审批与审计               |
| 永久保存群聊             | 隐私与存储压力           | 默认短期内存；长期只存摘要；群级明确开启                      |
| 单机故障                 | QQ 与运维入口同时离线    | 自动重启、异机备份、保留 SSH/控制台应急通道                   |

## 16.2 开工前需要确认

- 部署环境：Linux 发行版、CPU/内存，以及 NapCat 是否与 Minecraft 同机。

- 机器人 QQ：是否已有专用小号，准备加入哪些群，哪些群管理员能调节人格。

- Minecraft：服务端类型与版本（Paper/Fabric/Forge 等）、运行方式（Docker/systemd/面板）和现有备份方案。

- LLM：使用云 API 还是本地模型；可接受的月度预算与内容发送边界。

- 数据库：已有 MySQL/MariaDB 还是由本项目新建；备份目标放在哪里。

- 吐槽人格：毒舌程度、禁区、称呼、表情使用、夜间规则和希望机器人参与的话题。

- 运维范围：MVP 除 Minecraft 外是否还要管理其他服务；哪些动作绝对不允许通过 QQ 执行。

> **建议立即冻结的决定：**协议 OneBot 11、框架 NoneBot2、反向 WebSocket、Docker Compose、白名单用户（群聊/私聊双通道）唯一运维入口、结构化运维动作、高风险二次确认、群消息默认不永久保存。其余参数均可在 PoC 后调整。

# 附录 A：来源与调研依据

以下资料用于本次选型。产品需求中的概率、冷却、留存期、权限边界和里程碑属于本项目设计建议，不是上游项目承诺。

**\[S1\]** [<u>NapCat 官方文档：OneBot 11、HTTP/WebSocket/反向 WebSocket 能力</u>](https://napneko-napcatqq.mintlify.app/)

**\[S2\]** [<u>NapCat 官方 GitHub：基于 NTQQ 的现代协议端实现</u>](https://github.com/NapNeko/NapCatQQ)

**\[S3\]** [<u>NapCat 官方说明：通过 OneBot 约定对外提供 HTTP/WebSocket 接口</u>](https://napneko.github.io/guide/napcat)

**\[S4\]** [<u>NapCat 生产部署：网络、文件权限、认证与防火墙建议</u>](https://napneko-napcatqq.mintlify.app/guides/deployment)

**\[S5\]** [<u>NoneBot OneBot 适配器：OneBot v11 反向 WebSocket（推荐）</u>](https://onebot.adapters.nonebot.dev/docs/guide/setup/)

**\[S6\]** [<u>NoneBot2 Matcher：Rule、Permission、priority 与 block</u>](https://nonebot.dev/docs/api/matcher)

**\[S7\]** [<u>NoneBot2 数据库最佳实践：SQLAlchemy/Alembic 与 MySQL DSN</u>](https://nonebot.dev/docs/best-practice/database/)

**\[S8\]** [<u>Hermes Agent API Server：OpenAI 兼容 HTTP API、本机绑定与 Bearer Key</u>](https://hermes-agent.nousresearch.com/docs/user-guide/features/api-server)

**\[S9\]** [<u>Hermes Agent 安全：身份、危险命令审批、容器隔离、MCP 凭据过滤</u>](https://hermes-agent.nousresearch.com/docs/user-guide/security)

**\[S10\]** [<u>Hermes Agent 终端后端：local、docker、ssh 等</u>](https://hermes-agent.nousresearch.com/docs/user-guide/configuration)

**\[S11\]** [<u>Hermes Agent MCP：连接外部工具服务器与工具过滤</u>](https://hermes-agent.nousresearch.com/docs/user-guide/features/mcp)

**\[S12\]** [<u>Hermes Agent 平台适配器开发指南</u>](https://hermes-agent.nousresearch.com/docs/developer-guide/adding-platform-adapters)

**\[S13\]** [<u>Paper server.properties：Management Protocol 的 host、secret 与 TLS 配置</u>](https://docs.papermc.io/paper/reference/server-properties/)

# 附录 B：术语

| **术语**      | **含义**                                                                 |
|---------------|--------------------------------------------------------------------------|
| NapCat        | 基于 NTQQ 的 QQ Bot 协议端，本项目用作接入层。                           |
| OneBot 11     | 聊天机器人应用接口标准，本项目中是 NapCat 与 NoneBot2 的线协议。         |
| NoneBot2      | Python 异步机器人框架，承载插件、权限、事件处理和应用逻辑。              |
| Matcher       | NoneBot2 的事件响应器，可组合规则、权限、优先级和阻断行为。              |
| Ops Gateway   | 本项目自建的受控运维服务，只提供结构化白名单动作。                       |
| Hermes Agent  | Nous Research 的 Agent 运行时，可通过 API、工具/MCP 和终端后端执行任务。 |
| 硬触发/软触发 | 硬触发要求明确回应；软触发仅作为主动发言候选。                           |
| 群服互联      | QQ 群与 Minecraft 服务器聊天、事件和身份的双向桥接。                     |
